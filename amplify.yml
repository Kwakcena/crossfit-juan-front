version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm use
        - npm install
    build:
      commands:
        - nvm use
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*
